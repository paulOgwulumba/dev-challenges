/**
 * Given an array containing n distinct numbers from 0 to n, return a sorted array of the missing numbers.
 * Example:
 * findMissingNumbers([0, 1, 3], 3) // [2]
 * findMissingNumbers([9, 6, 4, 3, 5, 7, 0, 1], 10) // [2, 8]
 */

export function findMissingNumbers(arr: number[], n: number): number[] {

}

